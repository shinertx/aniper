[workspace]
members = ["executor"]
resolver = "2"

# Pin zeroize to 1.3.0 to resolve conflict between Solana 1.18 (zeroize <1.4) and reqwest/rustls (zeroize 1.6).
# Until Solana updates, we force the lower version universally.
[workspace.dependencies]
zeroize = "1.3.0" 