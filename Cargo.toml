[workspace]
members = ["executor"]
resolver = "2"

# Note: Zeroize version conflict between Solana (requires <1.4) and reqwest (requires ^1.6.0)
# This will need to be resolved when Solana updates or with dependency overrides in CI 