[workspace]
members = ["executor"]

# Patch to reconcile Solana 1.18 (zeroize <1.4) with reqwest/rustls (zeroize 1.6).
# Until Solana updates, we force the lower version universally.
[patch.crates-io]
zeroize = "=1.3.0" 